meta {
  name: Ready
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ready
  body: none
  auth: none
}

tests {
  test("returns 200 when both Postgres and Redis are reachable", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().data.status).to.equal("ready");
  });
}
